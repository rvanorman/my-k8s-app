name: my-k8s-app
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
description: A Python program to deploy a web application onto a Kubernetes cluster
config:
  pulumi:tags:
    value:
      pulumi:template: webapp-kubernetes-python
